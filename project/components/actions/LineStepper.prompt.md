Line stepper for a planning price or tolerance: a short row of values with the active one strong.

```jsx
<LineStepper values={['$360','$380','$400','$420']} value="$400" onChange={setPrice} activeColor="var(--commodity-wheat)" />
```
