Two separate 24px pills that never merge: provenance (how a figure was captured) and confirmation (counterparty state).

```jsx
<ProvenanceChip kind="ocr-medium" />
<ConfirmationChip kind="disputed" />
```
